# PR A2 Plan: Wording Pass

Status: opened 2026-09-08 on branch `arap-restructure-a2`, stacked on `arap-restructure-a1`; all stages committed by 2026-09-16, each reviewed and approved. A2 changes the wording of normative text and therefore does not claim to be script-verifiable. Every changed unit is reviewed by a person against `PASS0-INVENTORY.tsv`, which records each unit's actor, condition, obligation, and exceptions; the scripts in the plan's Appendix X run as evidence, not as pass or fail. Nothing in A2 may settle a question recorded in `PROTOCOL-GAPS.md`.

## Inputs

- `PASS0-INVENTORY.tsv`: the ten rows whose `a1_dest` differs from `dest` are A2's relocation work; the consolidation candidates deferred to A2 are listed in `PASS0-REPORT.md`.
- `PASS0-REPORT.md`: the reading-order items recorded during A1 execution and its two reviews.
- A scan of Part I for paragraphs that break the plan's prose rules: fourteen paragraphs exceed four sentences or carry more than two RFC 2119 keywords (listed in stage A2.3).
- The seven editor's notes and the Section 22 pointer list inserted by A1.

## Stages

Each stage is one commit and a review checkpoint.

### A2.1 Structural completions

Status: applied in the working tree on 2026-09-08 and corrected after two reviews; not committed, awaiting sign-off. Corrections from the second review: the core `denial` definition states both REQUIRED cases and the OPTIONAL case itself, and Section 14 keeps only the bundle coverage rule; the Section 10 verification sub-bullet carries an obligation verb again (one added MUST, so the keyword total is 409); the relocated `evaluation_id` verification rule names the Access Request Service; the three relocated bullets in Sections 11 and 13 are re-sentenced to stand in their new context; Section 11's introduction claims only what its rules support and cites where the remaining shared-state rules live; the forms permission sits after both definitions in Section 20 so the definition list renders whole, and the two self-references inside that section are removed; the `binding_token` definition points to both forms; the Section 7 transitions table cites Section 14 for `partial`. Build clean; no missing anchors; 408 keyword occurrences; the unit comparison against the committed A1 text lists exactly the reworded lead-ins (240, 344, 742, 785), the relocated units (243, 245, 347, 349, 656 as a sentence, 744, 788, 921 with its actor, 937), the two added citations (176, 641), and the reworded `items` sub-entry. Item 5 changed: the `partial` mapping row (709) stays in Appendix C and its inventory destination is corrected.

Complete the structure A1 could not, because doing so requires rewording a lead-in or a table row.

1. Create Section 11, Shared-State Deployments, after Core Conformance, holding verbatim the `evaluation_id` by-reference form (259), the both-forms paragraph (261), the lookup pattern (817), and the `evaluation_id` verification path (1096). Reword the three lead-ins they leave (256, 815, 1094) so each still states its complete rule and points to Section 11 for the alternative form.
2. Move the two bulk `denial` coverage bullets (359, 361) to Section 14 and reword the lead-in at 356 to cover the single-item case and point to Section 14.
3. Move the broadened-scope bullet (861) to Section 13 and reword the lead-in at 858.
4. Move the PDP forms permission (1080) to Section 20 with its actor made explicit.
5. Express the `partial` transition (table row 689) as a sentence in Section 14; the Section 7 table keeps its other rows. The `partial` mapping row (709) stays in the Appendix C table; its inventory destination is corrected to C, since the table is illustrative.
6. Remove the six-step list from the Introduction (the diagram replaces it) and give the sentence "This profile defines that protocol layer" its antecedent back.
7. Replace "below" in the request `items` definition with a citation and drop the pointer sentence A1 added for it. Give the status-mapping sentence in Section 7 a citation to its explanation in Appendix C.
8. Add citations at the first Part I uses of `client.actor` and `approval.state`, which are defined later.
9. Remove the Section 5 editor's note about Section 11, now resolved.

### A2.2 Security Considerations

Status: signed off by the editor on 2026-09-08 after two reviews and committed. Second-review corrections: the confused-deputy paragraph cites both denial-verification paths (signed token, and the `evaluation_id` path for shared state), names bundle item order, and moves its register reference into an editor's note in the document's convention; the binding-token paragraph points at the bulk and other integrity-protected forms rather than the hashed form; a bulk-bundle escalation paragraph is added for the two per-item rules in Section 14; the surfaces paragraph uses the cited section's injection framing for approval material; the task-handle paragraph names the separate cancellation authorization; the on-behalf paragraph and the availability paragraph cite the endpoint-protection rules; the denial paragraph names the two fail-closed rules; anchors added to Endpoint Protection and Denial Binding Alternatives. First-review corrections: the approval-reference paragraph preserves the by-value versus lookup distinction and cites the completion section for both, with the shared-state section cited only for the lookup pattern itself; the confused-deputy paragraph describes denial-binding verification (Subject, Resource, Action, Context), task binding (a service conformance rule, now anchored as `ars-processing-rules`), and the PDP scope check as three separate mitigations and says the requester and client identity comparison is an open register question; the approver-eligibility paragraph says an ineligible approval could violate policy unless local policy explicitly allows and records the exception. The section has four groupings matching the original (Decision and Binding Integrity; Policy and Approver Hygiene; Information Disclosure; Operational and Integration) and fourteen threat paragraphs, each drawn from the threat sentence that introduced the corresponding original subsection and each citing the section that now holds the mitigating rule. Five anchors were added to headings the paragraphs cite. The section contains no RFC 2119 keyword; the keyword total is unchanged at 409; the unit comparison against the A2.1 commit is empty; all cross-references resolve; build clean.

Replace the A1 pointer list with threat statements: one short paragraph per threat naming what an attacker could do and citing the section whose rules mitigate it. No RFC 2119 keyword in the section. Source material is the threat sentences that A1 carried with their rules and the current pointer list. Citation-only rule: every threat statement cites an existing rule and implies no check that no section mandates; a threat with no mitigating rule in the text is a register item, not a Security paragraph. The Section 22 editor's note is removed in this stage; the five register-linked editor's notes stay.

### A2.3 Part I prose and deferred consolidations

Status: reviewed and approved by the editor on 2026-09-16 and committed. What was done, checkable against the unit comparison with the A2.2 commit (49 units added, 25 removed, 8 reported as moved because Section 4 was renamed):

Review corrections applied on 2026-09-10: the polling list has a neutral lead-in, with the `pending` condition retained only on the permission to poll; the backoff, `Retry-After`, stopping, and notification rules retain their original conditions. The roles paragraph describes responsibilities independently of representation, followed by a qualified description of the signed artifacts (`binding_token`, not the entire denial response) and citations to the other integrity-protected formats and by-reference alternatives. These corrections add no requirements and leave the keyword total at 405.

Verification after those corrections: the comparison still reports 49 added, 25 removed, and 8 moved units against A2.2; the aggregated keyword count is 405, with zero in Security Considerations; no cross-reference target is missing; all example blocks are unchanged. Both profiles rebuilt successfully as HTML and text, with submission-stream metadata and text-layout warnings. The regenerated HTML confirms the neutral polling lead-in, the `pending` condition only in the first bullet, and the qualified signed-artifact paragraph.

- Fourteen Part I paragraphs restructured without changing any actor, condition, obligation, or exception: multi-obligation paragraphs became bulleted lists under a lead-in that names the actor (the submission authentication rules, the freshness rules, the polling rules, the re-evaluation checks, the `approval.state` JWS rules, the `next_action` handling); the others were split into two or three shorter paragraphs. The freshness rules are a bulleted list, not a numbered one, so as not to imply the ordering that register item G10 records as undefined. The Part I scan now reports no paragraph over four sentences or two keywords outside lists.
- Duplicate threat sentences removed from seven rule paragraphs whose threat is now stated in Security Considerations (task handles, idempotency keys, availability, approver eligibility, approval replay, confused deputy, approval-reference substitution), plus the attacker sentence in the URL-trust paragraph and the two rationale sentences in the `binding_token` integrity paragraph. Only sentences with no obligation and no condition were removed.
- Consolidations: the actor-preservation bullet in Delegation (formerly 1131) is removed and the two citations that depended on it (the inline binding claim and the exact-match baseline) now point at the PEP conformance rule that states it; the two Access Request Service sentences of the callback-security paragraph (formerly 1258) are removed as restatements of the callback endpoint and authentication rules, leaving the PEP sentence. Keyword total 405, down four, exactly those two consolidations. 1155 and 188 are left as they are.
- Registry: the plan said the Description column would point to Section 8. Applied as a lead-in sentence stating that the semantics and defaults are normative in the completion section and the table registers and summarizes the names; the cells are unchanged, since a registry table needs standalone descriptions. Recorded as a deviation.
- Section 4 renamed to Roles, Trust, and Keys, with an opening paragraph naming the three roles and the deployment shapes, and the uniform-applicability paragraph moved here from the Terminology entry (the definition itself stays in Terminology).
- Deferred to a follow-on sub-stage, A2.3b: the one-definition-list, one-sentence-per-member rule for member definitions whose explanation has grown into a paragraph, because it touches every member definition in Sections 5, 6, and 8 and deserves its own review.

- Tighten the fourteen flagged paragraphs, identified by their opening words rather than line numbers, since A2.1 has already shifted lines: "For cross-vendor interoperability", "The `binding_token` member round-trips", "An autonomous PEP MUST verify that these URLs", "The Access Request Service SHOULD retain Idempotency-Key state", "The Access Request Service MUST authenticate the PEP", "When the `binding_token` carries its own expiry", "When a task is `pending`, a PEP MAY poll", "Profiles of this specification MAY define additional completion modes", "The `approval` object MAY additionally include a `state` member", "The `evaluation_id` of the original denied evaluation is denial-binding material", "The PDP MUST NOT authorize a re-evaluation solely", "When `approval.state` is carried by value as a JWS", "A PEP MUST drive its behavior from `next_action`", "A PEP MUST NOT treat an Approval Result as authorizing". Split into shorter paragraphs or numbered steps without changing actor, condition, obligation, or exception. The freshness procedure and the `next_action` rules are the two that most need it.
- Deferred consolidations, decided: 1131 into 1058, with the three citations at 860, 1180, 1181 reworded to point at Section 10 (this one needs the closest review, since it relocates a permission three citations depend on); the service sentences of 1258 into 921 and 931; the reason-code registry Description column pointing to Section 8; 1155 and 188 are left as they are, because 188 is conditional and 1155 is not.
- Section 4, decided: add the roles paragraph the plan intended (the three deployment shapes, currently in Terminology) and rename to the plan's title, Roles, Trust, and Keys.
- Apply the one-definition-list, one-sentence-per-member rule where a member's explanation has grown into a paragraph; move "why" sentences to Design Rationale. (Deferred to A2.3b; see status above.)

### A2.3b Member definitions

Status: reviewed and approved by the editor on 2026-09-16 and committed. Thirty-three definition-list entries in Part I were scanned. Twelve already had the intended shape. Of the nineteen with longer bodies, most carry obligations that belong with the member, so the rule is applied as: the body opens with the presence condition, the type, and one descriptive sentence, and each obligation follows as its own sub-bullet inside the definition. Five entries were restructured that way (`binding_token` and `context` in the denial and submission objects, `evaluation_id` in the `denial` object, `status_endpoint` in the Task Handle, `partial` in the status enumeration); the sub-bullets render as a list inside the definition and the surrounding definition lists stay whole. Two wording changes: the implementation-guidance sentence in the denial-side `template` definition moved to the Design Rationale entry on `template`; the denial-side `expires_at` definition no longer repeats the rejection rule verbatim stated in the submission-side definition and the freshness rules, and points to both instead (keyword total 404, down one for that duplicate). This is safe despite the earlier decision not to consolidate the three clock-skew sentences at 240, 400, and 1097: that decision rejected merging them into one sentence that would have dropped the configured-tolerance condition, whereas here the two surviving rules keep that condition verbatim and only the denial-side duplicate is removed; G10 and G19 are untouched. The remaining twelve longer entries are unchanged because each sentence is a condition, an obligation, or the description. Unit comparison against the A2.3 commit: 7 units removed, 16 added, all the entries named here; build clean; all cross-references resolve.

### A2.4 Appendices and history

Status: reviewed and approved by the editor on 2026-09-16 and committed; A2 is complete. Added: a non-normative framing paragraph at the head of Appendix B; a Design Rationale entry on why the signed form is presented first and shared state as the alternative; a -01 Document History entry describing the restructure and the catalog split. No editor's note removed. Corrected after review: the rationale entry no longer says shared state "does not work across an organizational boundary" or that it "replaces each artifact"; it says the by-reference alternatives depend on trusted shared or delegated state that independently implemented roles cannot assume, and can be used separately or together with the signed forms, matching Section 11 and the completion rules. All three additions are keyword-free; the unit comparison against the A2.3b commit is empty; keyword total 404; build clean.

- Appendix B: a framing sentence for the moved scenario bullets and Design Goals.
- Appendix D: one new entry on why the signed form is presented first and shared state as the alternative.
- Document History entry for the restructure.
- Editor's notes: none is removed in A2.4. The register-linked notes stay until their items are resolved outside A2; the Section 22 note goes in A2.2.

### A2.5 End-to-end review corrections

Status: applied 2026-09-17 from two independent reviews of the complete A2 result (a cold implementer read of the rendered text and a hunk-by-hunk audit of the A1-to-A2 diff); reviewed and approved by the editor on 2026-09-17 and committed. The audit found no change in any requirement's actor, condition, obligation, or force across the 42 hunks and judged all ten consolidations equivalent. The implementer read passed the 80% test. Corrections applied, all keyword-free or within units whose meaning is unchanged: the two one-item lists left by moving the by-reference form and the lookup pattern to Shared-State Deployments are recast as prose; the two A1 pointer sentences that sat between a lead-in colon and its list (the `access_request` members and the verification steps) are moved after the list or folded into the lead-in; the Security Considerations subsection that duplicated the Section 8 heading "Decision and Binding Integrity" is renamed "Denial and Approval Integrity"; the aggregation subsection gains the anchor `bulk-aggregation` and the three citations inside Bulk Submissions that pointed at the section's own top now point at it, with one redundant self-citation dropped; the Cancellation section no longer cites itself; the idempotency threat statement no longer claims correlation, which the body does not say; the roles pointer for other integrity-protected formats cites Denial Binding Alternatives; the submission section cites Bulk Submissions at the first use of `items`, cites Machine-Readable Forms at `request_schema_url`, and gains a pointer sentence naming `items` and `callback`; the Protocol Overview regains the sentence that the PEP can poll, receive a callback, or otherwise determine completion, lost when the six-step list became the diagram; "the mapping" in the status-mapping SHOULD names its antecedent; the lifecycle paragraph names the diagram it cites steps of; the callback section's event-subscription paragraph moves after the callback material; the unsupported-cancellation sentence no longer says "any cancellation endpoint"; the two `subject.properties.act` exclusions also cite the `client.actor` definition; the circular parenthetical in the by-reference rule is removed; the Emergency Access subsection regains, as a keyword-free sentence, the context that `emergency` is a well-known optional member of `requested_access`, which the A1 relocation had left behind under that object's lead-in. Pre-existing findings went to the register as version 9: evidence added to G10 and G15, and new entries G20, G21, and G22. The editor's review of the register text corrected all four additions before approval: complementary binding obligations with different actors are not defects and the identity question stays under G6; the shared-state freshness rule does select its value, so G10 now states the precise open case; a silently ignored `callback` is not visible to a PEP that skips polling, so G21 cross-references G18 and no longer infers optional support from member presence; G22 is narrowed to audit-correlation guidance, since withdrawing the reuse permission would be a protocol change. Structure follow-ups noted for a later decision, not applied: the bulk hash construction under Denial Binding Alternatives, Emergency Access under Delegation, rules inside the Terminology entry for Authorization-Relevant Context, and four non-identical restatements (actor-chain rejection, `subject.properties.act` exclusion, default approval scope, form members).

## Acceptance for each stage

- A relocated bullet or table row is re-sentenced to stand in its new context, naming its actor; every subsection has a lead-in; a reader of the destination alone can tell what the unit governs. This rule was added after A2.1's first review found four relocated bullets standing as orphaned fragments.

- A reviewer reads every changed unit against its inventory row and confirms actor, condition, obligation, and exceptions are unchanged, or records the change as intended with a reason.
- The Appendix X scripts run as evidence: the unit comparison lists the changed units and nothing else; the anchor script reports no missing target; the aggregated counter's total is explained by the recorded changes; both profiles build clean.
- No text in A2 resolves a `PROTOCOL-GAPS.md` item. Where wording brushes one, the editor's note stays.

## Section numbering

Section 15 (Idempotency Extended Guidance) is dropped: Pass 0 found nothing for it and A2 adds nothing. Plan section codes remain as identifiers; the rendered document numbers sections consecutively, so plan code 16 onward renders one lower. Citations in the text use anchors and are unaffected.

## Out of scope

Protocol changes (PR B), the register's Part A items, and any new requirement. If a wording change would need one, it stops and goes to the register.
